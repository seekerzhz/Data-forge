Given the problem statement below, generate a complete runnable Python generator script.

CLI contract (must follow exactly):
- Accept arguments: `--id <int>` and `--output-dir <path>`
- When invoked once with `--id N`, write exactly one file: `<output-dir>/<N>.in`
- Do NOT write other case files in that invocation
- Create `output-dir` if missing
- Do not print the generated input to stdout; write it to the required file with `Path(args.output_dir, f"{args.id}.in").write_text(...)`
- Use only the Python standard library (`argparse`, `random`, `pathlib`, etc.)
- Make generation reproducible: seed RNG with a fixed function of `--id` (for example `random.seed(0x41534349 ^ id)`)
- Keep input format strictly valid for the statement constraints
- Output Python code only, no explanation

Case design guidance for each `--id` value in `1..{{total_cases}}`:
- ids `1..{{small_cases}}`: small / easy-to-debug
- next {{special_cases}} ids: special / edge / extreme patterns
- next {{random_cases}} ids: random medium-scale
- remaining ids up to {{total_cases}}: near upper-limit stress tests

Problem statement:
{{problem}}
